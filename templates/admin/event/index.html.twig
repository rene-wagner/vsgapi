{% extends 'admin/layout.html.twig' %}

{% block title %}Veranstaltungen - VSG Admin{% endblock %}

{% block content %}
    <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-md-between gap-3 mb-4">
        <h1 class="h3 mb-0">Veranstaltungen</h1>
        <a href="{{ path('admin_event_new') }}" class="btn btn-primary">Neue Veranstaltung</a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div data-controller="event-calendar"></div>
        </div>
    </div>
{% endblock %}