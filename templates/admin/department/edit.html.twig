{% extends 'admin/layout.html.twig' %}

{% block title %}{{ department.name }} bearbeiten - VSG Admin{% endblock %}

{% block content %}
    <div class="main-header">
        <h1>{{ department.name }} bearbeiten</h1>
        <a href="{{ path('admin_department_index') }}" class="btn btn-secondary">Zurück</a>
    </div>

    <div class="card">
        {{ include('admin/department/_form.html.twig') }}
    </div>
{% endblock %}
