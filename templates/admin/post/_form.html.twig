{% form_theme form 'form_div_layout.html.twig' %}

{{ form_start(form, { attr: { class: 'space-y-6' } }) }}
    {{ form_errors(form) }}

    <div>
        {{ form_label(form.title, null, { label_attr: { class: 'mb-2 block text-sm font-medium text-gray-900' } }) }}
        {{ form_widget(form.title, { attr: { class: 'block w-full rounded-lg border border-gray-300 bg-gray-50 px-3 py-2.5 text-sm text-gray-900 focus:border-blue-500 focus:ring-blue-500' } }) }}
        <div class="mt-2 text-sm text-red-600">{{ form_errors(form.title) }}</div>
    </div>

    <div
        data-controller="easymde"
        data-easymde-improve-url-value="{{ path('admin_post_rewrite') }}"
        data-easymde-csrf-token-value="{{ csrf_token('post_rewrite') }}"
    >
        <div class="mb-2 flex items-center justify-between gap-3">
            {{ form_label(form.content, null, { label_attr: { class: 'mb-0 block text-sm font-medium text-gray-900' } }) }}
            <button
                type="button"
                class="inline-flex items-center rounded-lg border border-blue-700 px-3 py-2 text-sm font-medium text-blue-700 hover:bg-blue-50 focus:outline-none focus:ring-4 focus:ring-blue-300 disabled:cursor-not-allowed disabled:opacity-60"
                data-easymde-target="button"
                data-action="click->easymde#improve"
            >
                <i class="fa-solid fa-robot" aria-hidden="true"></i><span class="ml-2">Text ausformulieren</span>
            </button>
        </div>
        {{ form_widget(form.content, { attr: { class: 'markdown-input block w-full rounded-lg border border-gray-300 bg-gray-50 px-3 py-2.5 text-sm text-gray-900 focus:border-blue-500 focus:ring-blue-500', 'data-easymde-target': 'input' } }) }}
        <div class="mt-2 hidden text-sm text-red-600" data-easymde-target="error"></div>
        <div class="mt-2 text-sm text-red-600">{{ form_errors(form.content) }}</div>

        <div id="postRewriteModal" tabindex="-1" aria-labelledby="postRewriteModalLabel" aria-hidden="true" class="fixed inset-0 z-50 hidden overflow-y-auto overflow-x-hidden p-4" data-easymde-target="modal">
            <div class="fixed inset-0 bg-gray-900/60" aria-hidden="true"></div>

            <div class="relative z-10 flex min-h-full items-center justify-center">
                <div class="w-full max-w-5xl">
                    <div class="relative flex max-h-[calc(100vh-2rem)] flex-col overflow-hidden rounded-lg bg-white shadow-xl">
                        <div class="flex items-center justify-between rounded-t border-b border-gray-200 px-4 py-3 md:px-5">
                            <h2 class="text-lg font-semibold text-gray-900" id="postRewriteModalLabel">Textvorschlag</h2>
                            <button type="button" class="inline-flex h-8 w-8 items-center justify-center rounded-lg text-gray-400 hover:bg-gray-100 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500" data-action="click->easymde#closeModal" aria-label="Schließen">
                                <svg class="h-4 w-4" aria-hidden="true" fill="none" viewBox="0 0 14 14" stroke="currentColor" stroke-width="1.5">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m1 1 12 12M13 1 1 13" />
                                </svg>
                            </button>
                        </div>
                        <div class="min-h-0 flex-1 overflow-y-auto p-4 md:p-5">
                            <div class="py-8 text-center" data-easymde-target="modalLoading">
                                <svg class="mx-auto h-8 w-8 animate-spin text-gray-400" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 0 1 8-8v4a4 4 0 0 0-4 4H4Z"></path>
                                </svg>
                                <p class="mt-3 text-sm text-gray-500">Text wird ausformuliert…</p>
                            </div>

                            <div class="hidden rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700" data-easymde-target="modalError"></div>

                            <div class="hidden space-y-4" data-easymde-target="modalBody">
                                <div class="rounded-lg border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800" role="alert">
                                    Verwendetes Modell:
                                    <a href="#" target="_blank" rel="noopener noreferrer" class="font-medium underline hover:no-underline" data-easymde-target="modelLink">
                                        <span data-easymde-target="modelName"></span>
                                    </a>
                                </div>

                                <div>
                                    <label for="post_rewrite_preview" class="mb-2 block text-sm font-medium text-gray-900">Vorschlag</label>
                                    <textarea id="post_rewrite_preview" class="block w-full" data-easymde-target="previewInput"></textarea>
                                </div>

                                <details class="rounded-lg border border-gray-200 bg-gray-50">
                                    <summary class="cursor-pointer px-4 py-3 text-sm font-medium text-gray-900">System-Prompt</summary>
                                    <div class="border-t border-gray-200 px-4 py-3">
                                        <pre class="whitespace-pre-wrap break-words text-sm text-gray-700" data-easymde-target="prompt"></pre>
                                    </div>
                                </details>
                            </div>
                        </div>
                        <div class="flex items-center justify-end gap-3 rounded-b border-t border-gray-200 px-4 py-3 md:px-5">
                            <button type="button" class="rounded-lg border border-gray-300 bg-white px-5 py-2.5 text-sm font-medium text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-4 focus:ring-gray-200" data-action="click->easymde#closeModal">Abbrechen</button>
                            <button type="button" class="rounded-lg bg-blue-700 px-5 py-2.5 text-sm font-medium text-white hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-300 disabled:cursor-not-allowed disabled:opacity-60" data-easymde-target="applyButton" data-action="click->easymde#apply" disabled>Übernehmen</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="grid gap-6 md:grid-cols-2">
        <div>
            {{ form_label(form.hits, null, { label_attr: { class: 'mb-2 block text-sm font-medium text-gray-900' } }) }}
            {{ form_widget(form.hits, { attr: { class: 'block w-full rounded-lg border border-gray-300 bg-gray-50 px-3 py-2.5 text-sm text-gray-900 focus:border-blue-500 focus:ring-blue-500' } }) }}
            {% if form.hits.vars.help %}
                <p class="mt-2 text-sm text-gray-500">{{ form.hits.vars.help }}</p>
            {% endif %}
            <div class="mt-2 text-sm text-red-600">{{ form_errors(form.hits) }}</div>
        </div>

        <div>
            {{ form_label(form.author, null, { label_attr: { class: 'mb-2 block text-sm font-medium text-gray-900' } }) }}
            {{ form_widget(form.author, { attr: { class: 'block w-full rounded-lg border border-gray-300 bg-gray-50 px-3 py-2.5 text-sm text-gray-900 focus:border-blue-500 focus:ring-blue-500' } }) }}
            <div class="mt-2 text-sm text-red-600">{{ form_errors(form.author) }}</div>
        </div>
    </div>

    <div class="grid gap-4 md:grid-cols-2">
        <label class="flex items-start gap-3 rounded-lg border border-gray-200 bg-gray-50 p-4 text-sm text-gray-900">
            {{ form_widget(form.published, { attr: { class: 'mt-0.5 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500' } }) }}
            <span>
                <span class="block font-medium">{{ form.published.vars.label }}</span>
            </span>
        </label>

        <label class="flex items-start gap-3 rounded-lg border border-gray-200 bg-gray-50 p-4 text-sm text-gray-900">
            {{ form_widget(form.oldPost, { attr: { class: 'mt-0.5 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500' } }) }}
            <span>
                <span class="block font-medium">{{ form.oldPost.vars.label }}</span>
            </span>
        </label>
    </div>
    <div class="text-sm text-red-600">{{ form_errors(form.published) }}</div>
    <div class="text-sm text-red-600">{{ form_errors(form.oldPost) }}</div>

    <div>
        <span class="mb-2 block text-sm font-medium text-gray-900">{{ form.categories.vars.label }}</span>
        <div class="space-y-3 rounded-lg border border-gray-200 bg-gray-50 p-4">
            {% for child in form.categories %}
                <label class="flex items-center gap-3 text-sm text-gray-900">
                    {{ form_widget(child, { attr: { class: 'h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500' } }) }}
                    <span>{{ child.vars.label }}</span>
                </label>
            {% endfor %}
        </div>
        <div class="mt-2 text-sm text-red-600">{{ form_errors(form.categories) }}</div>
    </div>

    <section class="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm">
        <div data-media-selector>
            <div class="border-b border-gray-200 px-6 py-4">
                <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                    <h2 class="text-sm font-semibold text-gray-900">Bild</h2>
                    <div class="flex flex-wrap gap-2">
                        <button type="button" class="inline-flex items-center rounded-lg border border-blue-700 px-3 py-2 text-sm font-medium text-blue-700 hover:bg-blue-50 focus:outline-none focus:ring-4 focus:ring-blue-300" data-media-selector-open>
                            <i class="fa-solid fa-image" aria-hidden="true"></i><span class="ml-2">{{ 'Auswählen'|trans }}</span>
                        </button>
                        {% if post.picture %}
                            <a href="{{ path('admin_mediathek_item_edit', { id: post.picture.id }) }}"
                               class="inline-flex items-center rounded-lg border border-blue-700 px-3 py-2 text-sm font-medium text-blue-700 hover:bg-blue-50 focus:outline-none focus:ring-4 focus:ring-blue-300"
                               aria-label="Bearbeiten" title="Bearbeiten">
                                <i class="fa-solid fa-pen-to-square" aria-hidden="true"></i><span class="ml-2">Bearbeiten</span>
                            </a>
                        {% endif %}
                        <button type="button" class="inline-flex items-center rounded-lg border border-red-700 px-3 py-2 text-sm font-medium text-red-700 hover:bg-red-50 focus:outline-none focus:ring-4 focus:ring-red-300" data-media-selector-remove {% if not post.picture %}disabled{% endif %}>
                            <i class="fa-solid fa-trash" aria-hidden="true"></i><span class="ml-2">{{ 'Entfernen'|trans }}</span>
                        </button>
                    </div>
                </div>
            </div>
            <div class="px-6 py-4">
                {% if post.picture %}
                    {{ include('admin/_media_item_card.html.twig', {
                        media_item: post.picture,
                        media_url: media_url,
                        wrapped: false,
                        show_edit_action: false,
                        title: '',
                    }) }}
                {% else %}
                    <div data-media-selector-preview data-media-selector-preview-mode="card"></div>
                {% endif %}
                {{ form_widget(form.picture) }}
            </div>
        </div>
    </section>

    <div class="flex items-center gap-3">
        <button type="submit" class="inline-flex items-center justify-center rounded-lg bg-blue-700 px-5 py-2.5 text-sm font-medium text-white hover:bg-blue-800 focus:outline-none focus:ring-4 focus:ring-blue-300">Speichern</button>
        <a href="{{ path('admin_post_index') }}" class="inline-flex items-center justify-center rounded-lg border border-gray-300 bg-white px-5 py-2.5 text-sm font-medium text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-4 focus:ring-gray-200">Abbrechen</a>
    </div>
{{ form_end(form) }}
