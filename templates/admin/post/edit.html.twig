{% extends 'admin/layout.html.twig' %}

{% block title %}{{ post.title }} bearbeiten - VSG Admin{% endblock %}

{% block content %}
    <div class="main-header">
        <h1>{{ post.title }} bearbeiten</h1>
        <a href="{{ path('admin_post_index') }}" class="btn btn-secondary">Zurück</a>
    </div>

    <div class="card">
        {{ include('admin/post/_form.html.twig') }}
    </div>
{% endblock %}
