ai:
    agent:
        post_rewrite:
            platform: 'ai.platform.openrouter'
            model:
                name: '%env(OPENROUTER_MODEL)%'
                options:
                    temperature: 0.3
            prompt: '%app.post_rewrite_prompt%'
